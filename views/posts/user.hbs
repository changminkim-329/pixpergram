<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Experiences</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
        integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"
        integrity="sha384-B4gt1jrGC7Jh4AgTPSdUtOBvfO8shuf57BaghqFfPlYxofvL8/KUEfYiJOMMV+rV"
        crossorigin="anonymous"></script>
    <link rel="stylesheet" href="stylesheet/posts.css">
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="/">Pixpergram</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" style="font-weight: 200;" href="/">Home <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" style="font-weight: 200;" href="/posts">Experiences</a>
                </li>
                {{#if currentUser}}
                    <li class="nav-item active">
                    <a class="nav-link" style="font-weight: 200;"  href="/user/{{currentUser.username}}">My Experiences</a>
                </li>
                {{/if}}
            </ul>
            <ul class="navbar-nav ml-auto">
                {{#unless currentUser}}
                <li class="nav-item">
                    <a class="nav-link" style="font-weight: 200;" href="/login">로그인</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" style="font-weight: 200;" href="/register">회원가입</a>
                </li>
                {{else}}
                <li class="nav-item">
                    <a class="nav-link" style="font-weight: 200;" href="/user/{{currentUser.username}}">{{currentUser.username}}님</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" style="font-weight: 200;" href="/logout">로그아웃</a>
                </li>
                {{/unless}}
            </ul>
        </div>
    </nav>
    <div class="container">
        <br>
        <br>
            {{#if posts}}
            {{#each posts}}
            <div class="row text-center" style="display:flex; flex-wrap:wrap;">
            <div class="col-md-3 col-sm-6">
                <div class="thumbnail">
                    <img src="{{this.image}}" width="200" height="200">
                    <div class="caption">
                        <h4>{{this.name}}</h4>
                    </div>
                    <p>
                        <a href="/posts/{{this._id}}" class="btn btn-dark">더 알아보기</a>
                    </p>
                </div>
            </div>
            </div>
            {{/each}}
            {{else}}
            <div class="row" style="font-size: 30px; font-weight:300;">{{currentUser.username}}님의 경험을 올려주세요.</div>
            <br>
            <div class="row">
                <a class="btn btn-light btn-large" href="/posts/publish">경험 올리기</a>
            </div>
            {{/if}}
        </div>
    </div>
</body>

</html> 